# Aufgabe 1
Erstelle die Klasse ``LinkedList`` mit den Methoden 
* ``int size()``, 
* ``boolean contains(E element)``, 
* ``void addFirst(E element)``, 
* ``void addLast (E element)`` und 
* ``void printList()`` 

Die Klasse ``Node`` ist gegeben und muss nicht implementiert werden.